import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iotmeterops.app',
  appName: 'MeterOps',
 webDir: 'dist/iot-meterops-app/browser'
};

export default config;
